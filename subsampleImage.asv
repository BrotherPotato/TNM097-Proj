function [outputArg1] = subsampleImage(imgTile, targetTile, samplesPerTile)
sPT = sqrt(samplesPerTile); %Inte supereffektivt men den är mer intiutiv me tinks

disp("Image subsample tile")
disp(imgTile)

[rows, cols, ~] = size(imgTile);

% Calculate the size of each axis part
partRows = rows / sPT;
partCols = cols / sPT;

averages = zeros(sPT, sPt)

partition = 



end

